# Craftbeerpi4 Plugin to have a button style MQTT actor

mqttButton is similar to a mqttActor but resets itself within 1 second and sends a none-retained MQTT message only when being activated.

## Installation: 
install from repo: sudo pip3 install https://github.com/Alcoinus/cbpi4-mqttButton/archive/main.zip